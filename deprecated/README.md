# Deprecated / historical material

Nothing under this directory is an active build, deployment or operating instruction for the
tevriqorg fork.

Contents:

- `HANDOFF-legacy-upstream-2026-09-23.md` — the previous large living handoff, preserved intact for
  historical search/provenance after the active handoff was reset to current state only.
- `driverkit/` — the earlier DriverKit microphone proof of concept. It was blocked by DriverKit
  provisioning/entitlement requirements and superseded by the working `mic/` pipeline.

Historical files may contain old bundle identifiers, signing identities, release URLs, commands and
deployment assumptions. Consult the root `AGENTS.md` and `HANDOFF.md` before reusing anything.

By ChatGPT
